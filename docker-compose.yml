version: '3'

services:
  app-xml:
    build: .
    container_name: gerador-xml-app
    restart: always
    ports:
      - "3005:8501" # Porta 3005 no servidor -> 8501 no container
    volumes:
      - .:/app
    networks:
      - npm_default

networks:
  npm_default:
    external: true